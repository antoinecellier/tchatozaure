{
  "name": "tchatozaure",
  "version": "1.0.0",
  "private": true,
  "repository": "git@github.com:antoinecellier/tchatozaure.git",
  "license": "MIT",
  "scripts": {
    "clean": "yarn clean:yarn.lock && yarn clean:node_modules",
    "clean:node_modules": "rm -rf node_modules web/node_modules shared/node_modules mobile/node_modules",
    "clean:yarn.lock": "rm -rf yarn.lock web/yarn.lock shared/yarn.lock mobile/yarn.lock"
  },
  "workspaces": {
    "packages": [
      "shared/",
      "mobile/",
      "web/"
    ]
  }
}
